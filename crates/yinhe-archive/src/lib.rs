/// Archive support for .zip, .7z, .tar files (placeholder). List MIDI file entries in an archive.
pub fn list_midi_entries(_path: &str) -> Result<Vec<String>, &'static str> {
    Err("Archive support not yet implemented")
}
