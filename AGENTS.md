# 本项目名为yinhe，是一个黑乐谱MIDI编辑器

## 自定义 AI 生成提交信息的风格

1. 提交信息必须以动词开头，例如 "fix"、"feat" 等。可以在后面加括号，代表哪里修改了。
2. 提交信息必须包含一个短的描述，例如 "修复 bug"、"添加新功能" 等。
3. 提交信息必须包含一个空行，然后是更详细的描述。
4. 每行前面有一个短-号，例如 "- 修复 bug"、"- 添加新功能" 等。
5. 标题也用中文写。
6. 修改内容旁可能会有相应的注释，功能如何描述以注释为优先。
7. 每次修改代码后，都进行一次commit。如果修改过程分多个独立步骤，则每一步都commit一次。如果有功能写坏了，可以随时git回滚。

## 开发时，请注意代码质量

1. 如果某功能正常运行，并且它有已经存在的旧版fallback功能，一定要删。
2. 如果你看到有两个地方有着复用的代码，一定要告诉我一声，让我看看。
3. 我最喜欢用最简单高效的方式实现功能。所以我写代码的时候可能前后会用好几种方式，一直在迭代。当我发现哪个最好用的时候，如果还存在旧版冗余低效实现，或者仅微弱优化的函数，请告诉我，我可能会想删。
4. 我不太喜欢缓存，尽量不要引入缓存。我也特别不喜欢阈值，不要在不同阈值范围内做不一样的事。
5. 如果你觉得我话说得不清楚，有关键信息没告诉你，你必须停下告诉我你需要得知什么消息。不要自己想当然就改。
6. 有关黑乐谱的地方，你可以问我。我写的这个MIDI编辑器需要支撑1亿音符的编辑。用户可能在任何地方任何时间编辑，随机性很强。
7. 你看代码之后，顺便和我说一下这个地方是不是屎山太严重，比如是否遵守高内聚低耦合，DRY原则，是否需要优化性能（时间复杂度过高），是否需要添加单元测试/集成测试、代码是否过长（如果过长的话，要勇敢拆分成多个rs文件）。如果你是刚改完代码，那建议你每次改完代码都检查一下是否做到。
8. 不要写mod.rs，要用现代写法。
9. 有的算法，比如A转换到B转换到C，为什么不直接A转换到C？避免写这种多余转换。
10. 不要让代码出现上帝对象。如果出现，告诉我应该怎么拆。
11. 电脑里有ripgrep，想用可以随时用。
12. 既有的编译错误、警告以及测试错误请务必告诉我，哪怕和你的修复无关。我会考虑修复。
13. /Users/jieneng/Music/MIDIs 是我们常见的MIDI测试用例目录。/Users/jieneng/Music/Soundfonts/Starry Studio Grand v2.7~/Presets/A_Standard/Studio Grand - Standard (No Hammer).sfz 是我们常用的音色库。如果需要进行实际测试，一般使用这两个目录。
14. 如果要查找库，请优先查找本地。其次是网络。
15. 每次修改代码后，顺便编译出release，这样方便用户在会话结束后直接开始测试功能。
16. Windows主线程栈是1MB，macOS是8MB。因为要满足多平台，所以不要让主线程栈超过1MB。
17. 千万不要在生产环境中写unwrap()。没保存工程就完蛋了。
18. 如果哪里屡次容易出bug，就针对这个bug加个测试。
19. 该程序有一些缩写或专有名词。PR-Pianoroll-钢琴卷帘，AR-Arrangement-工程走带，AM-Automation-自动化。gate-音符长度（单位为tick）。
20. 每次修改之后，都跑一次cargo clippy，消除warning。如果有既有warning，报告给用户。
21. 每次修改代码后，都运行一次cargo fmt（默认代码风格即可）。大范围的格式化要单独提交，不要和功能改动混在一个commit里。
22. 我叫节能降耗，小末是我的开发者朋友，经常会给我提意见，如果看到这两个人名，就知道我们是在讨论关于代码的问题。
23. 验证时不要跑整个 workspace 的测试/clippy（太慢）。默认只跑本次修改涉及的 crate 及其下游依赖：`cargo test -p <crate>`、`cargo clippy -p <crate>`。只有改动跨 crate 公共接口（如 serde 派生、类型定义、公共 API 签名）时才把下游 crate 一起跑。全量测试留给 CI 或大改动前。
